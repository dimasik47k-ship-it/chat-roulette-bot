# 🚀 Деплой бота 24/7

## Варианты бесплатного хостинга

### 1. 🟢 Render.com (РЕКОМЕНДУЕТСЯ)
**Плюсы**: Бесплатно, автодеплой из GitHub, простая настройка
**Минусы**: Засыпает после 15 мин неактивности (решается UptimeRobot)

#### Шаги:
1. Зарегистрируйтесь на [render.com](https://render.com)
2. Создайте новый **Web Service**
3. Подключите GitHub репозиторий
4. Настройки:
   - **Build Command**: `npm install`
   - **Start Command**: `node src/bot-final.js`
   - **Environment**: Node
5. Добавьте переменную окружения:
   - `TELEGRAM_BOT_TOKEN` = ваш токен
6. Deploy!

**Важно**: Добавьте в `package.json`:
```json
{
  "engines": {
    "node": ">=18.0.0"
  }
}
```

---

### 2. 🔵 Railway.app
**Плюсы**: $5 бесплатно каждый месяц, не засыпает
**Минусы**: Требует привязку карты (не списывают)

#### Шаги:
1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. New Project → Deploy from GitHub
3. Выберите репозиторий
4. Добавьте переменную `TELEGRAM_BOT_TOKEN`
5. Railway автоматически определит Node.js и запустит бота

---

### 3. 🟣 Fly.io
**Плюсы**: Бесплатно, не засыпает, быстрый
**Минусы**: Требует установку CLI

#### Шаги:
1. Установите Fly CLI:
```bash
# Windows (PowerShell)
iwr https://fly.io/install.ps1 -useb | iex

# Linux/Mac
curl -L https://fly.io/install.sh | sh
```

2. Войдите:
```bash
fly auth login
```

3. Создайте `fly.toml` в корне проекта:
```toml
app = "your-bot-name"

[build]
  builder = "heroku/buildpacks:20"

[env]
  NODE_ENV = "production"

[[services]]
  internal_port = 8080
  protocol = "tcp"

  [[services.ports]]
    port = 80
```

4. Деплой:
```bash
fly launch
fly secrets set TELEGRAM_BOT_TOKEN=ваш_токен
fly deploy
```

---

### 4. 🟠 Glitch.com
**Плюсы**: Очень простой, не требует GitHub
**Минусы**: Ограничения по ресурсам

#### Шаги:
1. Перейдите на [glitch.com](https://glitch.com)
2. New Project → Import from GitHub
3. Вставьте ссылку на репозиторий
4. Откройте `.env` и добавьте:
```
TELEGRAM_BOT_TOKEN=ваш_токен
```
5. Бот запустится автоматически

---

### 5. 🔴 Heroku (Платный с 2022)
**Минусы**: Больше не бесплатный ($7/мес)

---

## 🛡️ Защита от засыпания (для Render)

Render засыпает после 15 минут неактивности. Решение:

### UptimeRobot (бесплатно)
1. Зарегистрируйтесь на [uptimerobot.com](https://uptimerobot.com)
2. Add New Monitor:
   - Type: HTTP(s)
   - URL: ваш URL от Render
   - Interval: 5 минут
3. Сохраните

UptimeRobot будет пинговать бота каждые 5 минут, не давая ему заснуть.

---

## 📦 Подготовка к деплою

### 1. Создайте файл `package.json` (если нет):
```json
{
  "name": "chat-roulette-bot",
  "version": "1.0.0",
  "description": "Telegram Chat Roulette Bot",
  "main": "src/bot-final.js",
  "scripts": {
    "start": "node src/bot-final.js",
    "dev": "node src/bot-final.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "node-telegram-bot-api": "^0.66.0",
    "better-sqlite3": "^11.0.0",
    "natural": "^8.0.1",
    "dotenv": "^16.4.5"
  }
}
```

### 2. Создайте `.gitignore`:
```
node_modules/
.env
*.db
*.log
```

### 3. Измените БД на файловую (для сохранения данных):

В `src/bot-final.js` замените:
```javascript
const db = new Database(':memory:');
```

На:
```javascript
const db = new Database('bot.db');
```

### 4. Загрузите на GitHub:
```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git push -u origin main
```

---

## 🎯 Быстрый старт (Render)

1. **Создайте репозиторий на GitHub** и загрузите код
2. **Зарегистрируйтесь на Render.com**
3. **New → Web Service**
4. **Connect GitHub** → выберите репозиторий
5. **Настройки**:
   - Name: `chat-roulette-bot`
   - Build Command: `npm install`
   - Start Command: `node src/bot-final.js`
6. **Environment Variables**:
   - `TELEGRAM_BOT_TOKEN` = ваш токен
7. **Create Web Service**
8. **Настройте UptimeRobot** (см. выше)

✅ Готово! Бот работает 24/7!

---

## 🔧 Альтернатива: VPS (если нужна полная стабильность)

### Бесплатные VPS:
- **Oracle Cloud**: Бесплатно навсегда (2 VM)
- **Google Cloud**: $300 на 90 дней
- **AWS**: 12 месяцев бесплатно

### Платные VPS (дешевые):
- **Contabo**: от €4/мес
- **Hetzner**: от €4.5/мес
- **DigitalOcean**: от $4/мес

---

## 📊 Мониторинг

### Логи на Render:
- Откройте Dashboard → ваш сервис → Logs
- Смотрите логи в реальном времени

### Логи на Railway:
- Откройте проект → Deployments → View Logs

---

## ⚠️ Важные замечания

1. **База данных**: Используйте файловую БД (`bot.db`), а не in-memory
2. **Переменные окружения**: Всегда используйте `.env` для токенов
3. **Мониторинг**: Настройте UptimeRobot для Render
4. **Бэкапы**: Периодически скачивайте `bot.db` с сервера

---

## 🆘 Нужна помощь?

Если нужны данные для деплоя (токены, ключи API), дайте знать!
