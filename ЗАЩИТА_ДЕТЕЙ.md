# 🛡️ Защита детей и безопасность

## ⚠️ ВАЖНО

Этот бот предназначен для общения между незнакомыми людьми. Безопасность пользователей, особенно несовершеннолетних, является приоритетом.

## 🔒 Реализованные меры безопасности

### 1. Возрастные группы
При регистрации пользователи выбирают возрастную группу:
- 13-17 (подростки)
- 18-24 (молодые взрослые)
- 25-34 (взрослые)
- 35-44 (зрелые)
- 45+ (старшие)

**Рекомендация**: Добавить фильтр, чтобы подростки (13-17) общались только с подростками.

### 2. AI-модерация
- Автоматическая проверка всех текстовых сообщений
- Блокировка токсичного контента (оценка >= 5)
- Детекция спама и ссылок
- Предупреждения при подозрительных сообщениях

### 3. Система жалоб
- Кнопка "🚫 Жалоба" в каждом чате
- 5 типов жалоб: спам, оскорбления, контент 18+, угрозы, другое
- Автобан при 5+ жалобах за 24 часа
- Теневой бан при 3+ жалобах

### 4. Быстрое завершение чата
- Кнопка "❌ Завершить" всегда доступна
- Кнопка "➡️ Следующий" для смены собеседника
- Возможность добавить в черный список

## 🚨 Что нужно добавить для полной безопасности

### 1. Фильтр по возрасту (КРИТИЧНО)
```javascript
// В функции поиска собеседника
if (u.age_group === '13-17') {
  // Искать только среди 13-17
  const partner = queue.find(q => {
    const pu = db.prepare('SELECT * FROM users WHERE id = ?').get(q.userId);
    return pu.age_group === '13-17';
  });
}
```

### 2. Модерация изображений
- Интеграция с API для проверки изображений (например, Google Cloud Vision API)
- Блокировка NSFW контента
- Автоматическая жалоба при обнаружении

### 3. Ограничения для несовершеннолетних
- Запрет на отправку фото/видео для группы 13-17
- Только текстовые сообщения и стикеры
- Дополнительные фильтры модерации

### 4. Родительский контроль
- Возможность для родителей заблокировать доступ
- Уведомления родителям (опционально)
- История чатов (анонимная, без содержимого)

### 5. Верификация возраста
- Дополнительная проверка для группы 18+
- Запрос документов (опционально)
- Поведенческий анализ

### 6. Логирование подозрительной активности
```javascript
// Пример
function logSuspiciousActivity(userId, reason) {
  db.prepare('INSERT INTO suspicious_logs (user_id, reason, timestamp) VALUES (?, ?, ?)').run(
    userId, reason, new Date().toISOString()
  );
  
  // Уведомление администратору
  bot.sendMessage(ADMIN_CHAT_ID, `⚠️ Подозрительная активность:\nUser: ${userId}\nПричина: ${reason}`);
}
```

### 7. Ограничение частоты сообщений
```javascript
// Анти-флуд для защиты от спама
const messageRateLimit = new Map();

function checkRateLimit(userId) {
  const now = Date.now();
  const userMessages = messageRateLimit.get(userId) || [];
  
  // Удаляем старые сообщения (старше 10 секунд)
  const recentMessages = userMessages.filter(t => now - t < 10000);
  
  if (recentMessages.length >= 5) {
    return false; // Превышен лимит
  }
  
  recentMessages.push(now);
  messageRateLimit.set(userId, recentMessages);
  return true;
}
```

## 📋 Рекомендации для администраторов

### 1. Мониторинг
- Регулярно проверяйте логи на подозрительную активность
- Следите за количеством жалоб
- Анализируйте заблокированные сообщения

### 2. Модерация
- Назначьте модераторов для ручной проверки жалоб
- Создайте чат для модераторов
- Быстро реагируйте на серьезные нарушения

### 3. Правила использования
- Создайте четкие правила (Terms of Service)
- Добавьте их в `/start` и `/help`
- Требуйте согласия при регистрации

### 4. Контакты для жалоб
- Добавьте email для жалоб: `abuse@yourdomain.com`
- Telegram канал для поддержки
- Форма для анонимных жалоб

## 🔧 Код для улучшения безопасности

### Фильтр по возрасту для подростков:
```javascript
// В обработчике 'find'
if (data === 'find') {
  if (activeChats.has(u.id)) return bot.sendMessage(chatId, '❌ Вы уже в чате!');
  if (queue.some(qu => qu.userId === u.id)) return bot.sendMessage(chatId, '⏳ Вы уже в очереди!');
  
  await bot.editMessageText('🔍 Ищем собеседника...', { chat_id: chatId, message_id: q.message.message_id });
  
  // НОВОЕ: Фильтр по возрасту для подростков
  let partner = null;
  
  if (u.age_group === '13-17') {
    // Ищем только среди подростков
    const teenIndex = queue.findIndex(qu => {
      const pu = db.prepare('SELECT * FROM users WHERE id = ?').get(qu.userId);
      return pu.age_group === '13-17';
    });
    
    if (teenIndex > -1) {
      partner = queue.splice(teenIndex, 1)[0];
    }
  } else {
    // Для взрослых - обычный поиск
    if (queue.length > 0) {
      partner = queue.shift();
    }
  }
  
  if (partner) {
    const pu = db.prepare('SELECT * FROM users WHERE id = ?').get(partner.userId);
    createChat(u, pu);
    
    await bot.sendMessage(chatId, '✅ Собеседник найден!\n\nНачинайте общение.', { reply_markup: chatMenu() });
    await bot.sendMessage(partner.telegramId, '✅ Собеседник найден!\n\nНачинайте общение.', { reply_markup: chatMenu() });
  } else {
    queue.push({ userId: u.id, telegramId: tgId, chatId, timestamp: Date.now() });
    updateUser(u.id, { status: 'in_queue' });
    
    setTimeout(() => {
      const idx = queue.findIndex(qu => qu.userId === u.id);
      if (idx > -1) {
        queue.splice(idx, 1);
        updateUser(u.id, { status: 'idle' });
        bot.sendMessage(chatId, '⏱️ Собеседник не найден. Попробуйте позже.', { reply_markup: mainMenu(u) });
      }
    }, 60000);
  }
  return;
}
```

### Блокировка фото для подростков:
```javascript
// В обработчике сообщений
if (msg.photo || msg.video || msg.video_note) {
  if (u.age_group === '13-17') {
    return bot.sendMessage(chatId, '🚫 Отправка фото и видео недоступна для вашей возрастной группы.\n\nЭто сделано для вашей безопасности.');
  }
}
```

## ⚖️ Юридические аспекты

### 1. Соответствие законам
- **COPPA** (США): Требует согласия родителей для детей до 13 лет
- **GDPR** (ЕС): Защита персональных данных
- **Закон о защите детей** (РФ): Ограничения на контент

### 2. Возрастные ограничения
- Рекомендуется: **13+** (как в Telegram)
- Лучше: **16+** или **18+**
- Добавьте предупреждение при регистрации

### 3. Ответственность
- Вы несете ответственность за контент
- Храните логи минимум 6 месяцев
- Сотрудничайте с правоохранительными органами

## 📞 Контакты для экстренных случаев

Если вы обнаружили:
- Детскую порнографию
- Угрозы жизни
- Торговлю людьми
- Другие серьезные преступления

**Немедленно свяжитесь с:**
- Полицией (102 в РФ, 911 в США)
- Роскомнадзором (для РФ)
- NCMEC (для США): www.cybertipline.org

## ✅ Чеклист безопасности

- [ ] Возрастные группы реализованы
- [ ] Фильтр по возрасту для подростков
- [ ] AI-модерация текста работает
- [ ] Модерация изображений (опционально)
- [ ] Система жалоб активна
- [ ] Автобан при 5+ жалобах
- [ ] Правила использования добавлены
- [ ] Контакты для жалоб указаны
- [ ] Логирование подозрительной активности
- [ ] Анти-флуд система
- [ ] Черный список работает
- [ ] Быстрое завершение чата доступно

## 🎯 Итог

Безопасность - это не одноразовая задача, а постоянный процесс. Регулярно обновляйте систему модерации, следите за новыми угрозами и быстро реагируйте на жалобы.

**Помните**: Лучше перестраховаться, чем потом сожалеть.
